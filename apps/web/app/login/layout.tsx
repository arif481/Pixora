import "../globals.css";

export const metadata = {
  title: "Sign In – Pixora",
  description: "Sign in to Pixora to start sharing photos with face recognition.",
};

export default function LoginLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
